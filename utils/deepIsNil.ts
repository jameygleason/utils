// import { isNil } from "./isNil"

// type Types = undefined | null | string | number | Array<any> | Object

// const empty = []
// let i = 0

// export function deepIsNil(arg: Types): any {
export function deepIsNil(): any {
	console.error("API incomplete. Currently unavailable.")
	//   try {
	//     if (isNil(arg)) {
	//       return isNil(arg)
	//     }
	//     if (arg.constructor === Array) {
	//       for (const a of arg) {
	//         const item = deepIsNil(a)
	//         if (item) {
	//           empty[i].push(a)
	//         }
	//       }
	//     }

	//     if (arg.constructor === Object) {
	//       for (const [k, v] of Object.entries(arg)) {
	//         const item = deepIsNil(v)
	//         if (item) {
	//           empty[i].push(Object.fromEntries([k, v]))
	//         }
	//       }
	//     }

	//     i++

	//     return [empty, null]
	//   } catch (err) {
	//     console.error(err)
	//   }
}
