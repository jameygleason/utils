// import { describe, it, expect } from "vitest"
// import { titleCase } from "../../dist/titleCase.js"

// const apStyle = [{ input: "", output: "" }]

// const chicagoManuelOfStyle = [{ input: "", output: "" }]

// describe("titleCase", () => {
// 	it.skip("TODO", () => {
// 		// noop
// 	})

// 	it.skip("Title cases string in the AP style", () => {
// 		for (const { input, output } of apStyle) {
// 			const [data, err] = titleCase(input)
// 			if (err !== null) {
// 				expect(err).to.eql(true)
// 			}
// 			expect(data).to.eql(output)
// 		}
// 	})

// 	it.skip("Title cases string in the Chicago Manuel of Style style", () => {
// 		for (const { input, output } of chicagoManuelOfStyle) {
// 			const [data, err] = titleCase(input)
// 			if (err !== null) {
// 				expect(err).to.eql(true)
// 			}
// 			expect(data).to.eql(output)
// 		}
// 	})
// })
